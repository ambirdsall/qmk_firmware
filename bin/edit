#!/usr/bin/env bash

main(){
  local root
  # shellcheck disable=SC2046 disable=SC2086
  root="$(dirname $(dirname ${BASH_SOURCE[0]}))"

  ${EDITOR} "$root/keyboards/zsa/ergodox_ez/keymaps/ergodox/keymap.c"
}

main "$@"
